var chai = require('chai');
var expect = chai.expect; // we are using the "expect" style of Chai
var gpio = require('./../gpio');
var fs = require('fs');

describe('scheduleRunner', function() {

  it('write a good test', function() {
  });
});